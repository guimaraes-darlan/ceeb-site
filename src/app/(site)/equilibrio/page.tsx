import { Metadata } from "next";
import EquilibrioPage from "./equilibrio-page";

export const metadata: Metadata = {
  title: "Equilíbrio Espiritual",
};

export default function Equilibrio() {
  return <EquilibrioPage />;
}
